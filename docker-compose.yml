services:
  backend:
    build: ./backend
    ports:
      - "8010:8000"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://localhost:8010/api/v1
    ports:
      - "3000:80"
    depends_on:
      - backend
