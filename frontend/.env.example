VITE_API_URL=http://127.0.0.1:8123/api/v1

# Optional. Only needed if the backend has FEDERATION_API_KEY set -- see
# backend/.env.example / README. Leave unset for local dev (default: no
# auth required on federation writes).
VITE_FEDERATION_API_KEY=
